let isPlayAudio = true;
let jsonData = {}; // json配置文件
let answerList = [];
let moleNum = 5;
let hammerNum = 5; // 默认锤子数
let finalScore = 0; // 最终得分

module.exports = {
    isPlayAudio,
    jsonData,
    answerList,
    moleNum,
    hammerNum,
    finalScore
}